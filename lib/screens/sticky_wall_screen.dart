import 'package:flutter/material.dart';

class StickyWallScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Sticky Wall')),
      body: Center(child: Text('Sticky Wall Screen')),
    );
  }
}
